import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

export const ResearchScreen = (props: Props) => {
  return (
    <View>
      <Text>ResearchScreen</Text>
    </View>
  )
}

const styles = StyleSheet.create({})